NEXT_PUBLIC_APP_URL=http://localhost:3000

# Supabase Auth
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Optional: empty keeps demo enabled in local dev and disabled in production.
NEXT_PUBLIC_ENABLE_DEMO_MODE=

# Supabase data access for server actions and profile sync
SUPABASE_SERVICE_ROLE_KEY=

# Cron job authentication (used by Vercel cron)
CRON_SECRET=
